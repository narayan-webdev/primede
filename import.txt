Sync -
inventory
status (active , inactive)\
content (product content)
price (can't have price more then 10x)
order details (whole saler (super admin) , accept reject btn to super admin)


Need to modify the existing project;

- Order Management
- Product Creation
- Create separate table (imported_products)